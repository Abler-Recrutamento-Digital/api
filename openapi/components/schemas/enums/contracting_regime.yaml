type: object
properties:
  data:
    type: array
    description: Registro serializado no formato JSON API    
    items: 
      oneOf:
        - $ref: '#/components/schemas/Enum' 
example:
  data:
    id: aprendiz
    type: contracting_regime
    attributes:
      name: Aprendiz
